# Streamlit Community Cloud checks for a requirements.txt in the entrypoint's
# OWN directory before falling back to the repo root -- this file exists so
# the deployed dashboard only installs what dashboard/app.py actually needs,
# instead of the full project's requirements.txt (which also pulls in
# nba_api, curl_cffi, fastapi/uvicorn, pytest, etc. for the ingestion and API
# layers the dashboard never touches). Trims real build time on every deploy.
#
streamlit==1.35.0
pandas==2.2.2
plotly==5.22.0
pyarrow==17.0.0
